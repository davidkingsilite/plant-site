import React from 'react';

const Missing = () => {
    return (
        <div>
            <h1>404</h1>
            <h3>File not found</h3>
            <p>The site configured at this address does not contain the requested file.<br/>

            If this is your site, make sure that the filename case matches the URL.<br/>
            For root URLs (like http://example.com/) you must provide an index.html file.<br/>

            Read the full documentation for more information about using GitHub Pages.</p>
        </div>
    )
}

export default Missing;
